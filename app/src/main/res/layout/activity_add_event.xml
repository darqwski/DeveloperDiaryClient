<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_add_event"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.darqwski.developerdiary.AddEventActivity"
    android:background="@color/colorBackgroundMain">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:id="@+id/AddEventEditText"
        android:layout_alignParentEnd="false"
        android:layout_alignParentStart="false"
        android:background="@drawable/border_radius_events"
        android:padding="5dp"
        android:textColor="#444444" />

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/AddEventGrid"
        android:layout_below="@+id/AddEventEditText"
        android:layout_alignParentStart="true"
        android:verticalSpacing="5dp"
        android:layout_marginTop="5dp"
        android:layout_above="@+id/addEventButton" />

    <Button
        android:text="Dodaj nową czynność"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/addEventButton"
        android:background="@drawable/button_brown_ripple"
        android:textColor="#FFF"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true" />

</RelativeLayout>
